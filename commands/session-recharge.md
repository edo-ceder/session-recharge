The /session-recharge command was invoked.

A UserPromptSubmit hook has already extracted fresh session memory and injected it into your context above this message. You do NOT need to run any scripts or read any files — the memory is already in your context.

Give the user a brief summary (2-3 sentences) of the session goal and current state, confirming you're back on track. Mention any active blockers or recent corrections.

$ARGUMENTS
